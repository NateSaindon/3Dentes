node_modules/
dist/
dist-ssr/
*.local
.DS_Store
.vite/

# Build products of tools/build-assets.mjs — regenerate with `npm run build:assets`.
# The source STLs in assets/cbct/stl/ ARE committed; these are derived from them.
public/dentition.glb
public/teeth.json

# Generated by vite-plugin-pwa at build time.
public/sw.js
public/workbox-*.js
public/registerSW.js

# CBCT imaging data and derived volumes. These live in ~/projects/3Dentes-cbct/,
# never in the repo: the USB mirror is 5.4 GB and the NRRDs 551 MB, both far past
# what belongs in git (and GitHub's per-file hard limit is 100 MB). Both are
# reproducible — the mirror from the USB, the NRRDs from tools/cbct/prepare.py.
*.nrrd
*.nrrd.gz
*.dcm
DICOMDIR
usb-mirror/
nrrd/

# Python bytecode from tools/cbct/
__pycache__/
*.pyc
